public class Encoding {
    public static String encode(String message, int key) {
        String encodedText = "";
        key %= 26;

        int length = message.length();


        return null;
    }
}
